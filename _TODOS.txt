README STUFF
    Python 3.9.6
    downloads and prepares basic datasets with size of in data folder (standard /data), which is set in job.sh
    creates _cache in data dir with size of ?? (or change cache_dir in run_experiment of Main.py) on first run if running all experiments. (for only creating cache first, set caching_only in run_experiment of Main.py to True)
    caching can be disabled: set use_caching in run_experiment of Main.py to False).
    further, use redo_cache for reinitalizing the caching.
    datasetcreation (infos for download etc)), datasetmanager (infos, spalten, anon), spalten klasse, vorverarbeitung (clean_and_split_data), dataloader (_set_types))
    for slurm run change settings in create_cluster in src/Main.py + in job.sh + select wanted parameters in run_all.sh

- squeue -u $USER | awk '!/spawner/ && NR>1 {print $1}' | xargs scancel
